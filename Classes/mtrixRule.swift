 
 
 
 
 
 

import UIKit
import Kingfisher

class mtrixRule: slidCircuitMega {
    var verlyBrowseSafe = false
    var chrAmountCard:kypdDisappearHard?
    var plTaskSelect:ClckedAmountData?
    var trverseNullHeight:ClckedAmountData?
    lazy var cmpleEqualSalary:UIImageView = {
        return UIImageView()
            .frscrePeriodZero(6.auto())
            .descrbeDetailObserve(true)
            .estercIncrementCaret(.scaleAspectFill)
            .bckspceRedrawBoth(.CnfrmRoomRange)
    }()
    
    lazy var frmAlignSequence:UIImageView = {
        let cmpleEqualSalary = UIImageView().estercIncrementCaret(.scaleAspectFit).dscssPriceDeselected(certnAgainProxy("268"))
        return cmpleEqualSalary
    }()
    
    lazy var cntrlWordShell:UILabel = {
        let keywrdTrueBegin = UILabel()
            .pertnCardMonth(tensnBottomSpeech([435, 431, 436, 431, 437, 436, 441, 384, 358, 442, 430, 427, 358, 440, 431, 441, 427, 358, 437, 428, 358, 429, 440, 443, 376, 376, 375, 377, 378, 375, 376]))
            .referenceCellPunch(.NtrdceSeriesBoth)
            .predctCrossKeep(2)
            .ccrcyOnceInvent(.systemFont(ofSize: 12, weight: .regular))
        return keywrdTrueBegin
    }()
    
    lazy var ltestTellDepend:UIView = {
        let ltestTellDepend = UIView()
        return ltestTellDepend
    }()
    
    lazy var nterveneRedrawScroll:UILabel = {
        let nterveneRedrawScroll = UILabel()
        return nterveneRedrawScroll
    }()
    
    lazy var chevePoolAlign:UIProgressView = {
        let chevePoolAlign = UIProgressView()
        chevePoolAlign.backgroundColor = .lbrryRemainPath(trnsfrmUnitUser: 0x4B505B)
        chevePoolAlign.progressTintColor = .ClckedContainsMicro
        chevePoolAlign.progress = 0.5
        return chevePoolAlign
    }()
    
    lazy var chrColorSequence : tchConfusePlus = {
        return tchConfusePlus().szeDictionaryRefresh(self, action: #selector(cmpleAlignEqual(button:)))
    }()
    var reserveMathExpect:DescendExpandSure?

    override func derelctAlphaCombine() {
        if let chrAmountCard = chrAmountCard{
            cntrlWordShell.text = chrAmountCard.replceLogicWait
            if crrgeCircleSwap{
                cntrlWordShell.font = .systemFont(ofSize: 20, weight: .regular)
            }
            cmpleEqualSalary.kf.setImage(with: URL(string: chrAmountCard.nknwnKiloPseudo))
            chrColorSequence.isSelected = chrAmountCard.ptmzeWrongAtomically
            var cpyrghtBottomHigh = 0
            nterveneRedrawScroll.rz.colorfulConfer { confer in
                if chrAmountCard.nstrctSlashInvoke == tensnBottomSpeech([404, 395, 413]) {
                    confer.text(chrAmountCard.nstrctSlashInvoke)?.font(.systemFont(ofSize: 8, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xFF6D1C))
                    cpyrghtBottomHigh += 1
                }
                if !chrAmountCard.exclsveWrongDate.isEmpty {
                    if cpyrghtBottomHigh > 0 {
                        confer.text(" | ")?.font(.systemFont(ofSize: 8, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xECECEC))
                    }
                    confer.text(chrAmountCard.exclsveWrongDate)?.font(.systemFont(ofSize: 8, weight: .bold)).textColor(.lbrryRemainPath(trnsfrmUnitUser: 0xECECEC))
                    cpyrghtBottomHigh += 1
                }
            }
            ltestTellDepend.isHidden = (cpyrghtBottomHigh == 0)
            chevePoolAlign.progress = Float(chrAmountCard.nstedThenBrush) / 100.0
        }
    }
    
    override func shdwUnsafeValidate() {
        contentView.addSubview(cmpleEqualSalary)
        contentView.addSubview(frmAlignSequence)
        contentView.addSubview(cntrlWordShell)
        contentView.addSubview(chevePoolAlign)
        contentView.addSubview(chrColorSequence)
        contentView.addSubview(ltestTellDepend)
        ltestTellDepend.addSubview(nterveneRedrawScroll)
        contentView.layer.backgroundColor =  UIColor.SelectedSecondCost.cgColor
        contentView.layer.cornerRadius = 5.auto()
        ltestTellDepend.frame = CGRect(x: 0, y: 0, width: self.frame.size.width, height: 24.auto())
        let fcsDecoderBound = CAGradientLayer()
        fcsDecoderBound.colors = [
          UIColor(red: 0, green: 0, blue: 0, alpha: 0).cgColor,
          UIColor(red: 0, green: 0, blue: 0, alpha: 1).cgColor
        ]
        fcsDecoderBound.locations = [0.070292, 1]
        fcsDecoderBound.startPoint = CGPoint(x:0.5, y: 0)
        fcsDecoderBound.endPoint = CGPoint(x: 0.5, y: 1)
        fcsDecoderBound.bounds = ltestTellDepend.bounds
        fcsDecoderBound.position = ltestTellDepend.center
        ltestTellDepend.layer.addSublayer(fcsDecoderBound)
        bckspceSideQuery()
    }
    
    override func updateConstraints() {
        super.updateConstraints()
        cmpleEqualSalary.snp.remakeConstraints { make in
            make.height.equalTo(160.auto())
            make.top.left.right.equalTo(contentView)
        }
        
        frmAlignSequence.snp.remakeConstraints { make in
            make.center.equalTo(cmpleEqualSalary)
            make.size.equalTo(CGSize(width: 24, height: 24))
        }
        
        chevePoolAlign.snp.remakeConstraints { make in
            make.left.right.equalToSuperview()
            make.top.equalTo(cmpleEqualSalary.snp.bottom)
            make.height.equalTo(2)
        }
        
        chrColorSequence.snp.remakeConstraints { make in
            if !chrColorSequence.isHidden {
                make.right.equalTo(-5.auto())
                make.top.equalTo(chevePoolAlign.snp.bottom).offset(7.auto())
                if verlyBrowseSafe {
                    make.size.equalTo(CGSizeMake(18, 18))
                }else{
                    make.size.equalTo(CGSizeMake(24, 24))
                }
            }
        }
        
        cntrlWordShell.snp.remakeConstraints { make in
            make.left.equalToSuperview().offset(5.auto())
            if chrColorSequence.isHidden {
                make.right.equalToSuperview().offset(-5.auto())
            }else{
                make.right.equalTo(chrColorSequence.snp.left).offset(-5.auto())
            }
            make.top.equalTo(chevePoolAlign.snp.bottom).offset(5.auto())
        }
        
        ltestTellDepend.snp.remakeConstraints { make in
            make.left.right.equalToSuperview()
            make.bottom.equalTo(cmpleEqualSalary.snp.bottom)
            make.height.equalTo(24.auto())
        }
        
        nterveneRedrawScroll.snp.remakeConstraints { make in
            make.center.equalTo(ltestTellDepend.snp.center)
        }
    }
}
