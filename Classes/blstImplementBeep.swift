 
 
 
 
 
 

import Foundation

extension bjctConceptSize {
    @objc func prpseRadiusCare(){
        guard let chrAmountCard = chrAmountCard else { return }
        self.dismiss(animated: false)
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) {
            frcWelcomeLine.nterfereBlueElapse(chrAmountCard: chrAmountCard,rejectCoreRepeat: "6")
        }
    }
    
    @objc func envrnDelayCombine(){
        guard let chrAmountCard = chrAmountCard else { return }
        self.dismiss(animated: false)
        if pertnAssignTest{
            _ = lvlProduceRoot.default.prdceAlphaPage(chrAmountCard: chrAmountCard,ncldeHaveDesire: true)
        }else{
            _ = lvlProduceRoot.default.prgressConvertStrike(chrAmountCard: chrAmountCard,ncldeHaveDesire: true)
        }
        NotificationCenter.default.post(name: .ptmzeModelMake.reglrBottomRefresh, object: nil)
    }
    
    @objc func lphPerformItem(){
        self.dismiss(animated: false)
    }
}
