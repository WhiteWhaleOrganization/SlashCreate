 
 
 
 
 
 

import Foundation

extension mtrixRule {
    func bckspceSideQuery(){
        if verlyBrowseSafe {
            chrColorSequence.kf.setImage(with: certnAgainProxy("82"), for: .normal)
            chrColorSequence.kf.setImage(with: certnAgainProxy("81"), for: .selected)
            chrColorSequence.isUserInteractionEnabled = false
        }else{
            chrColorSequence.isUserInteractionEnabled = true
            chrColorSequence.kf.setImage(with: certnAgainProxy("304"), for: .normal)
            chrColorSequence.kf.setImage(with: certnAgainProxy("304"), for: .selected)
        }
        setNeedsUpdateConstraints()
    }
    
    @objc func cmpleAlignEqual(button:tchConfusePlus){
        if verlyBrowseSafe {
            if let trverseNullHeight = trverseNullHeight {
                trverseNullHeight(chrAmountCard)
            }
        }else{
            if let plTaskSelect = plTaskSelect {
                plTaskSelect(chrAmountCard)
            }
        }
    }
}
