 
 
 
 
 
 

import UIKit

public class dirctNavigateShut: rdrwFragmentDisc {
    var lstExistEncode:[kypdDisappearHard] = []
    var verlyBrowseSafe = false
    var pertnAssignTest = false
    lazy var keybrdMagnifierPool = {
        let keybrdMagnifierPool = tchConfusePlus(type: .custom)
            .therefreComeAngle(selectDeflateKind(84))
            .therefreComeAngle(selectDeflateKind(170), .selected)
            .ccdentAssignDisk(.SrceShiftDecoder)
            .rerrngeStatusTask(.systemFont(ofSize: 14))
        return keybrdMagnifierPool
    }()
    lazy var envrnSubscribeForget = {
        let envrnSubscribeForget = tchConfusePlus(type: .custom)
            .therefreComeAngle(selectDeflateKind(676))
            .therefreComeAngle(selectDeflateKind(679), .selected)
            .ccdentAssignDisk(.SrceShiftDecoder)
            .rerrngeStatusTask(.systemFont(ofSize: 16))
        return envrnSubscribeForget
    }()
    lazy var rngTraceTrue = {
        let rngTraceTrue = tchConfusePlus(type: .custom)
            .therefreComeAngle(selectDeflateKind(282))
            .ccdentAssignDisk(.SrceShiftDecoder)
            .rerrngeStatusTask(.systemFont(ofSize: 16))
        return rngTraceTrue
    }()
    lazy var prdceExtendTrue = {
        let prdceExtendTrue = UIView().ccdentPairScreen(true).bckspceRedrawBoth(.lbrryRemainPath(trnsfrmUnitUser: 0x1A1C21))
        return prdceExtendTrue
    }()
    var perfrmSnapshotStrike:strmContrastGain = {
        let dellcReadyOnto = UICollectionViewFlowLayout()
        dellcReadyOnto.minimumLineSpacing = 10.auto()
        dellcReadyOnto.minimumInteritemSpacing = 10.auto()
        dellcReadyOnto.sectionInset = UIEdgeInsets(top: 10.auto(), left: 10.auto(), bottom: 10.auto(), right: 10.auto())
        let msmtchWidthRange = floor(((preserveScreenDialog - 10.auto() * 4)) / 3.0)
        dellcReadyOnto.itemSize = CGSizeMake(msmtchWidthRange, 201.auto())
        let perfrmSnapshotStrike = strmContrastGain(frame: .zero, collectionViewLayout: dellcReadyOnto)
        perfrmSnapshotStrike.backgroundColor = .clear
        perfrmSnapshotStrike.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: pctreRateRemain, right: 0)
        perfrmSnapshotStrike.register(mtrixRule.self, forCellWithReuseIdentifier: "mtrixRule")
        return perfrmSnapshotStrike
    }()
    
    public override func keywrdNameSince() {
        self.needIndentTime = "6"
        title = selectDeflateKind(79)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: keybrdMagnifierPool)
        keybrdMagnifierPool.sizeToFit()
        keybrdMagnifierPool.addTarget(self, action: #selector(pnnHugeParse), for: .touchUpInside)
        rngTraceTrue.addTarget(self, action: #selector(fregrndHeightWord), for: .touchUpInside)
        envrnSubscribeForget.addTarget(self, action: #selector(trverseReflowWish), for: .touchUpInside)
        view.addSubview(perfrmSnapshotStrike)
        view.addSubview(prdceExtendTrue)
        prdceExtendTrue.addSubview(rngTraceTrue)
        prdceExtendTrue.addSubview(envrnSubscribeForget)
        perfrmSnapshotStrike.delegate = self
        perfrmSnapshotStrike.dataSource = self
        NotificationCenter.default.addObserver(self, selector: #selector(cmpreUnlessReplay(no:)), name: .ptmzeModelMake.reglrBottomRefresh, object: nil)
    }
    
    public override func updateViewConstraints() {
        super.updateViewConstraints()
        perfrmSnapshotStrike.snp.remakeConstraints { make in
            make.left.right.top.equalToSuperview()
            if prdceExtendTrue.isHidden{
                make.bottom.equalToSuperview()
            }else{
                make.bottom.equalTo(prdceExtendTrue.snp.top)
            }
        }
        
        prdceExtendTrue.snp.remakeConstraints { make in
            if !prdceExtendTrue.isHidden{
                make.left.right.bottom.equalToSuperview()
                make.height.equalTo(60 + pctreRateRemain)
            }
        }
        
        envrnSubscribeForget.snp.remakeConstraints { make in
            if !prdceExtendTrue.isHidden{
                make.left.top.equalToSuperview()
                make.width.equalTo(prdceExtendTrue.snp.width).multipliedBy(0.5)
                make.height.equalTo(60)
            }
        }
        
        rngTraceTrue.snp.remakeConstraints { make in
            if !prdceExtendTrue.isHidden{
                make.right.top.equalToSuperview()
                make.width.equalTo(prdceExtendTrue.snp.width).multipliedBy(0.5)
                make.height.equalTo(60)
            }
        }
    }
    
    func nderlneDamageScroll(){
        var has = false
        for mo in lstExistEncode{
            if mo.ptmzeWrongAtomically {
                has = true
                break
            }
        }
        rngTraceTrue.setTitleColor(has ? .lbrryRemainPath(trnsfrmUnitUser: 0xEA4D3D) : .lbrryRemainPath(trnsfrmUnitUser: 0x828386), for: .normal)
        rngTraceTrue.isEnabled = has
    }
    
    @objc func cmpreUnlessReplay(no:Notification){
        self.cnnectBookLearn()
    }
    
    @objc func fregrndHeightWord(){
        for cntentSystemCreate in lstExistEncode{
            if cntentSystemCreate.ptmzeWrongAtomically && lvlProduceRoot.default.gestreDigitHave(nstntEveryShared: cntentSystemCreate.nstntEveryShared) != nil{
                _ = lvlProduceRoot.default.prgressConvertStrike(chrAmountCard: cntentSystemCreate,ncldeHaveDesire: true)
            }
        }
        cnnectBookLearn()
        nytmeUpdateInvoke(repntClickSize:!verlyBrowseSafe)
    }
    
    @objc func trverseReflowWish(){
        envrnSubscribeForget.isSelected = !envrnSubscribeForget.isSelected
        for cntentSystemCreate in lstExistEncode{
            cntentSystemCreate.ptmzeWrongAtomically = envrnSubscribeForget.isSelected
        }
        perfrmSnapshotStrike.reloadData()
        nderlneDamageScroll()
    }
    
    @objc func pnnHugeParse(){
        nytmeUpdateInvoke(repntClickSize:!verlyBrowseSafe)
    }
    
    func nytmeUpdateInvoke(repntClickSize:Bool){
        verlyBrowseSafe = repntClickSize
        keybrdMagnifierPool.isSelected = repntClickSize
        keybrdMagnifierPool.sizeToFit()
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: keybrdMagnifierPool)
        envrnSubscribeForget.isSelected = false
        prdceExtendTrue.isHidden = !repntClickSize
        for cntentSystemCreate in lstExistEncode{
            cntentSystemCreate.nmersRemainWeek = verlyBrowseSafe
            cntentSystemCreate.ptmzeWrongAtomically = false
        }
        perfrmSnapshotStrike.reloadData()
        self.view.setNeedsUpdateConstraints()
        nderlneDamageScroll()
    }
    
    public override func cnnectBookLearn() {
        lstExistEncode = lvlProduceRoot.default.cllbckRollTime()
        perfrmSnapshotStrike.reloadData()
    }
}
